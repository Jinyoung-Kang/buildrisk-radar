# 실데이터 검증 기록

- **1차 (2026-09-24)** — 6개 외부 API 키를 모두 넣고 전체 배치를 실제로 돌린 뒤 모든 API·화면을 점검 (문제 1~27)
- **2차 (2026-09-25)** — API · worker 분리, 보안, 공시 원문 구조화, 실거래, 주가 백테스트를 추가하고 같은 방식으로 점검 (문제 28~41, [아래](#2차-검증-2026-09-25))
- **3차 (2026-09-26)** — 모든 메뉴 · API 를 다시 훑고, 부하를 오래 걸어 메모리 · 캐시 · 데이터 해석까지 점검 (문제 42~61, [아래](#3차-검증-2026-09-26))

각 항목은 가능하면 **재현 테스트를 먼저 쓰고(실패 확인) → 고친 뒤 → 통과**시키는 순서로 처리했습니다.

## 적재 결과

| 항목 | 값 |
|---|---|
| DART 고유번호 | 119,447개사 (StAX 스트리밍 4.2초) · 종목코드 보유 3,994 · 현재 상장(유가·코스닥) 2,649 |
| 유니버스 | **43개사** (KSIC 41 종합건설 39 · 421 토목 기반조성 3 · 수동 포함 1) |
| 재무제표 | 보고서 946건 (OK 927 · NO_DATA 19), 원천 121,675행 → 표준화 13,815행, 2021Q1~2026Q2 |
| 공시 | 3,514건 (위험 유형 52건: 회생·부도·거래정지·감사의견) |
| 지역 | V-World 시군구 269개(일반구 포함) → 화면 단위 230곳 (일반구 합성 시 13곳) |
| 지역 통계 | 21,697행 (KOSIS 미분양 36개월 · R-ONE 매매/전세지수 37개월 · SGIS 2024 총가구) → 지역 지표 40,194건 |
| 경보 | 500건 (열린 경보 44) |
| DART 호출 | 5,349건 (일일 상한 15,000 이내) |

## 품질 지표

| 검사 | 결과 |
|---|---|
| 회계 항등식 자산 = 부채 + 자본 (927개 기업·기간) | 924 일치 · 3건 ±1원 (공시 원문 반올림) |
| 차입금 합 ≤ 부채총계 | 위반 0 |
| 계정 매핑률 (핵심 12개) | 91.5% — 자산·부채·자본·유동·매출·영업이익 100%, 영업현금흐름 99.9%, 이자비용 95.4%, 단기차입금 90.3%, 장기차입금 79.4%, 사채 33.5% (사채 미발행 회사가 많음) |
| 지역 코드 매핑률 | KOSIS 98.8% · R-ONE 100% (권역 합계 41건 제외) · SGIS 98.8% — 미매핑 6건은 모두 2026-07 인천 개편 사유 기록 |
| 골든 테스트 | 실제 응답 6건 (GS건설 · 계룡건설산업 · KCC건설, 2025 사업보고서 · 2026 반기) 항등식·스냅샷 통과 |

## 찾아서 고친 문제

### 배치 · 데이터 정합성

| # | 증상 | 원인 | 해결 | 고정 테스트 |
|---|---|---|---|---|
| 1 | 강제 종료 후 restart 한 기업개황 Job 이 COMPLETED 인데 **50건 누락** | `JdbcPagingItemReader` 가 '페이지 시작 키 + 페이지 안 건수'로 재시작하는데 WHERE 가 처리한 행을 빼 버려 미처리 행을 건너뜀 (850 mod 200 = 50) | 처리 여부로 걸러지는 Reader 는 `saveState(false)` — 재시작 지점을 DB 에서 도출 ([ADR-008](adr/008-restart-from-db-state.md)) | `CompanyProfileRestartIT` (수정 전 41건 누락으로 실패) |
| 2 | 컨테이너를 죽이면 실행이 STARTED 로 남아 **영원히 409** | 프로세스가 죽으면 JobRepository 상태를 아무도 정리하지 않음 | Step `last_updated` 를 하트비트로 보고 10분 넘게 멈추면 `JobOperator.recover()` 로 FAILED 처리 + 관리 API `POST /batch/executions/{id}/recover` + 화면 버튼 | `StaleExecutionIT` |
| 3 | 상장폐지 회사(건영·대아건설 등)가 유니버스에 포함될 뻔함 | corpCode.xml 은 상장폐지 회사에도 종목코드를 남김 (기업개황 corp_cls = E) | 유니버스 조건에 `corp_cls ∈ {Y, K}` 추가 | `ClassifierAndUniverseTest` |
| 4 | KOSIS 미분양 Job 실패 (err 31) | 한 요청 4만 셀 제한 — 36개월 한 번에 요청 | 6개월씩 나눠 요청 | 실데이터 재실행 |
| 5 | R-ONE Job NPE | 응답에 이름 없는 집계 행 3건 (`CLS_NM`·`CLS_FULLNM` null) | 이름 없는 행 건너뜀 | 실데이터 재실행 (FAILED → restart → COMPLETED) |
| 6a | 배치 모니터에 이미 해결된 FAILED(#7 강제 종료 검증, #15 R-ONE NPE)가 그대로 빨간색으로 남아 조치가 필요한지 알 수 없음 | 실행 이력은 감사 기록이라 지우면 안 되는데, 화면이 '같은 JobInstance 가 restart 로 완료됐는지'를 보여 주지 않음. 실패 메시지도 'Unable to process chunk' 뿐이라 원인이 안 보임 | 실행 이력 API 에 `resolvedBy`(이후 완료된 실행 번호) 추가 → "→ #9 재시작으로 해결" 표시, 실패 시 종료 메시지 맨 앞에 근본 원인 한 줄(`원인: DartApiException — …`), 정리된 실행에 정리 사유 기록 | `FinancialStatementJobIT` · `StaleExecutionIT` |
| 6 | 경계 파생 Step 실패 | PostGIS 의 GEOS 3.11 에 `ST_CoverageSimplify` 없음, 실패 후 대체 쿼리도 aborted 트랜잭션에서 실패 | GEOS 버전을 먼저 확인해 함수 선택 | restart 로 파생 Step 만 재실행 확인 |

### 회계 표준화 (골든 테스트로 발견)

| # | 실제 사례 | 해결 |
|---|---|---|
| 7 | '유동성사채'·'사채'를 **순액 줄 + 액면 줄**로 두 번 공시 → 사채 이중 집계 | 바로 앞 줄과 같은 이름이면 액면 줄로 보고 제외 |
| 8 | 유동 '회사채'(700억)·비유동 '회사채'(320억) — 이름이 같아 한 줄만 셀 뻔함 | 떨어져 있는 같은 이름은 합산 (7번 규칙은 인접 줄에만) |
| 9 | '유동성사채'에 차입금용 IFRS 태그(`CurrentPortionOfLongtermBorrowings`)가 붙어 단기차입금·사채 양쪽에 집계 | 해당 태그 규칙 제거, 한 원천 행은 한 표준계정에만 |
| 10 | GS건설은 본문에 차입금이 없고 '단기금융부채'·'장기금융부채'로 묶어 공시 | 우선순위 50 이상 = 대체 규칙 (본 규칙에 걸린 행이 없을 때만) |
| 11 | '단기차입금및유동성장기부채' · '유동 차입금(사채 포함)' · '차입금등(비유동)' 등 표기 변형 수십 가지 | 재무상태표 **부채 구간(유동/비유동) 판별** + 구간 규칙 (V6 마이그레이션) → 단기 81.6→90.3%, 장기 68.2→79.4% |
| 12 | 이자비용이 현금흐름표 '이자의 지급' 대체인지 근거에서 알 수 없음 | 지표 구성요소·R-C02 근거에 원천 계정(`CF:이자지급`) 표시 |

### 규칙 · 분류

| # | 증상 | 해결 |
|---|---|---|
| 13 | '주권매매거래정지**해제**(액면병합 주권 변경상장)'이 거래정지로 분류 → HIGH 경보 오탐 | 분류 사전에 중립 키워드(해제·액면병합·변경상장 등) — 재분류 후 경보 RESOLVED |
| 14 | 공시 규칙 경보가 재분류로 거짓이 돼도 EXPIRED 로 닫힘 | 창 안이면 RESOLVED, 창 밖이면 EXPIRED |

### 지역 매핑

| # | 증상 | 해결 |
|---|---|---|
| 15 | V-World 경계에 이미 인천 신설 구가 있는데 seed 의 '미매핑 사유' 항목이 이름 매칭보다 먼저 적용 | 수동 항목은 이름으로 못 찾을 때만 사유로 사용, `regionCd` 지정 시에만 강제 |
| 16 | R-ONE '전국'·'수도권'·'5대광역시'가 미매핑으로 집계 | 하위 이름 없는 최상위 합계 = AGGREGATE (매핑률 분모 제외) |

### API

| # | 증상 | 해결 |
|---|---|---|
| 17 | 비JSON 본문 · 허용 안 된 메서드 · 잘못된 Content-Type · `limit=-1` 이 **500** | 400 · 405 · 415 · 범위 보정 |
| 18 | 동시 실행 경합 시 `JobExecutionAlreadyRunningException` 이 500 | 409 JOB_ALREADY_RUNNING |
| 19 | 검증 메시지가 영어 ("must be less than…") — 컨테이너 로케일 | `spring.web.locale=ko_KR` 고정, 메시지를 `필드: 설명` 형식으로 |
| 20 | web(:3400) 경유 Swagger 의 서버 주소가 `http://app:8410`(도커 내부) → Try it out 실패 | OpenAPI servers 를 상대 경로 `/` 로 |
| 21 | 지역 수동 매핑 404 — KOSIS 코드에 `/` 가 있어 경로 변수로 못 받음 | `PUT /mapping/region-codes` 본문으로 받기 |
| 22 | 화면에서 추가한 매핑 규칙을 지울 방법이 없음 | `DELETE /mapping/account-rules/{id}` (USER 규칙만) |
| 23 | 대시보드 최신 공시일이 UTC 타임스탬프로 하루 어긋나 보임 | SQL 에서 날짜 문자열로 |

### 화면

| # | 증상 | 해결 |
|---|---|---|
| 24 | 카카오 지도가 한반도에 맞춰 확대되지 않음 | 컨테이너 크기가 정해진 뒤 `relayout()` → `setBounds`, ResizeObserver |
| 25 | '데이터 없음'과 '변화 거의 없음'(중립 회색)이 구분 안 됨 | 데이터 없는 지역은 흐리게 |
| 26 | R-ONE 조사 대상이 아닌 군 지역에 "REB_API_KEY 필요" 표시 | 문구 수정 |
| 27 | 로딩 중 순위 제목이 "값이 작은 순"으로 잘못 표시 · 빈 데이터에서 범례가 '0' 한 칸 | 정의 로드 전 중립 제목 · 빈 범례 숨김 |

## 쓰기 API 시나리오 (실데이터)

| 시나리오 | 결과 |
|---|---|
| `PUT /rules/R-C01` 임계 300→250 | v2 생성, 같은 값 재요청은 v2 유지, 잘못된 심각도 400, 토큰 없음 401 |
| 규칙 재평가 | v1 열린 경보 → CLOSED `RULE_CHANGED`, v2 경보 새로 생성 → 원래 값으로 v3 |
| `PATCH /alerts/{id}` ACK → OPEN | `ackedAt` 기록·해제 |
| 매핑 규칙 추가 · 잘못된 정규식 · 삭제 · seed 규칙 삭제 | 201 · 400 · 200 · 400 |
| 지역 수동 매핑 · 없는 지역 · 되돌리기 | MANUAL · 404 · UNMAPPED(사유 유지) |
| 같은 Job 동시 실행 | 두 번째 409 |
| 강제 종료 → recover → restart | 실행 #7 FAILED → #9 가 같은 JobInstance 로 이어감 |

---

## 2차 검증 (2026-09-25)

### 적재 결과

| 항목 | 값 |
|---|---|
| 공시 원문 (수주·해지·채무보증, 최근 1년) | 1,313건 — 구조화 1,312 · 일부만 1 (거래정지 공시가 보고서명에 '공급계약'을 포함 — 계약 서식이 아님) |
| 수주 계약 | 1,122건 (현재 739 · 정정으로 대체 360 · 해지 연결 1) — 공사 지역 시군구 922 (82%) · 시도만 93 · 해외 36 · 해석 불가 52 · 지역 없음 19 |
| 채무보증 | 170건 (PF 유형 표 66 · 정정 대체 26) — '총 잔액 = 보증 한도(미사용 포함)' 주석 85 · 사용 잔액 계산 가능 32 |
| 아파트 실거래 | 256 시군구 × 25개월 = 6,400 (시군구, 월) · 거래 1,032,741 + 해제 63,438 = 1,096,179행 (파티션 2024 · 2025 · 2026) → 화면 단위 월 집계 5,520 |
| 주가 | 43종목 × 2023-09-25 ~ 2026-09-22 = 31,138행 (주말 0행) |
| 새 경보 | R-C05 15 · R-R03 6(열림 1) · R-X01 1 |
| 호출 | DART 1,356 (원문 1,313 + 기타) · 실거래 6,929 · 주가 43 — 모두 자체 일일 상한 이내 |

### 찾아서 고친 문제

| # | 증상 | 원인 | 해결 | 고정 |
|---|---|---|---|---|
| 28 | worker 가 공시 원문 Job 과 주가 Job 을 동시에 시작하자 한쪽이 `could not serialize access due to read/write dependencies` 로 **FAILED** | Spring Batch JobRepository 의 실행 생성 격리수준 기본값 SERIALIZABLE | 같은 Job 중복은 큐 부분 유니크 인덱스 + advisory lock 이 막으므로 `isolation-level-for-create=read_committed` + 메타 테이블 충돌 3회 재시도 | `JobRequestQueueIT.서로_다른_Job_을_동시에_시작해도_둘_다_실행된다` — 수정 전 설정으로 되돌리면 5회 중 3번째에 같은 오류로 실패, 수정 후 통과 |
| 29 | 수주 공시 24건이 PARTIAL (계약명·계약일 없음) | 자율공시 서식은 계약명을 '세부내용'(해지는 '세부물건'), 계약일을 '계약(수주)일'로 씀. 해지의 '관련공시'에 여러 공시가 나열됨 | 파서 v2 — 항목 경로 추가, 관련공시에서 '공급계약 체결' 날짜를 고름 | 실제 원문 fixture 2건 추가. **재파싱 1,313건 3.8초 · DART 호출 0건** (호출 수 전후 1,356 동일) → PARTIAL 24 → 1 |
| 30 | 공사 지역 1,100건 중 시군구 784건(71%) | "경기도 용인시~경기도 화성시"(물결표), "부산시 북구"(광역시를 '시'로), "경기 용인"(접미사 없음), "평택시 고덕면 …"(시도 없음), "세종특별자치시 전동면"(시도 = 시군구 하나) | 경계 이름과 **정확히 하나가 맞을 때만** 매핑하는 규칙 추가 (추정 없음 — 전국에 둘인 '고성군'은 매핑 안 함, 2026-07 개편으로 없어진 '인천 서구'는 시도까지만) | `AddressRegionMatcherTest` 16 사례 → 922건(82%) |
| 31 | R-C05 가 15개사에서 발생 — 과다 의심 | 원문 확인 결과 값은 맞지만, 절반(85/170)이 '총 잔액은 채무보증 **한도**이며 미사용잔액 포함'이라는 주석을 담 | 파서 v3 — 주석 표기를 플래그로, 한도별 '미사용한도' 합을 읽어 **한도 합이 총 잔액과 ±1% 로 맞을 때만** 사용 잔액 계산. 규칙은 공시값 그대로, 근거·화면에 '한도 기준 · 사용 잔액' 표시 | `FilingParserTest` (IPARK: 한도 23,406억 ≈ 총 잔액 → 미사용 3,406억) · 맞지 않으면 비움 |
| 32 | 주가 차트의 경보 표시가 모두 2026-09 에 몰림 | 표시일로 `first_seen_at`(배치를 처음 돌린 날)을 씀 — 과거 분기 경보도 오늘 날짜가 됨 | 백테스트와 같은 point-in-time 기준(근거 공시 접수일 중 가장 늦은 날)으로 표시 | 화면 문구 · API |
| 33 | api 포트(8410)를 호스트에 열어 두면 `X-Forwarded-For` 를 위조해 레이트리밋·감사 로그의 IP 를 바꿀 수 있음 | Tomcat 기본 신뢰 프록시가 사설 대역 전체 — 도커 게이트웨이에서 온 요청의 XFF 를 믿음 | api 포트를 닫음 (→ 41번에서 불충분함을 발견) | 실측: 내부망 컨테이너가 보낸 위조 XFF 가 그대로 기록됨 |
| 34 | 규칙 평가 Job FAILED `syntax error at or near "NULLORDER"` | 텍스트 블록 + 상수 결합에서 공백 누락 (1차의 "WHEREc" 와 같은 유형) | 결합부 공백 | `RuleEvalJobIT` 가 먼저 잡음 (R-X01 이 수주 조회를 부름) |
| 35 | (실행 전 검토) 주가 수집 run 의 출처 'FSC_STOCK' 이 `collect_run.source varchar(8)` 초과 | — | 출처 코드 'FSC' | — |
| 36 | 경보 검증 화면: '제외' 열이 줄바꿈되어 행이 매우 길어짐, 차트 부제가 없는 라벨을 설명 | — | 제외 사유를 표본 수 아래 보조 줄로, 표본 수를 축 라벨(`R-C02 · n8`)로 | 화면 캡처 |
| 37 | Trivy: app 이미지의 내장 Tomcat 11.0.24 에 고칠 수 있는 CRITICAL 3건 (CVE-2026-65182 · 65905 · 68525) | Boot 4.1.1 BOM 의 Tomcat 버전 | 이 앱은 Tomcat 자체 인증기(DIGEST·FORM)·컨테이너 보안 제약을 쓰지 않지만(인증은 Spring Security) 11.0.25 로 올림 — BOM 이 Gradle platform 이라 해석 규칙으로 | CI `images` 잡의 Trivy(고칠 수 있는 CRITICAL 이면 실패) · 로컬 재검사 0건 · 테스트 139 통과 |
| 38 | **CodeQL** `js/client-side-unvalidated-url-redirection`: 로그인 후 `next` 로 이동 | '/' 로 시작하고 '//' 가 아니면 허용하던 문자열 검사 — `/\evil.example` 은 브라우저가 프로토콜 상대 주소로 해석해 **외부로 이동**(Node URL 로 재현) | `new URL(next, origin)` 으로 해석해 출처가 같을 때만 경로 부분 사용 | 수정 전후 5개 입력 비교 |
| 39 | **CodeQL** `java/tainted-arithmetic`: `GET /alerts?page=2147483647` 이 **500** | `page * size` int 오버플로 → 음수 OFFSET | `page ≤ 100,000` 검증(400) + long 으로 계산 | `ApiIT` (수정 전 실제 스택에서 500 확인 → 400) |
| 40 | **CodeQL** 나머지 4건 — 로그 위조 · 예측 가능한 임시 경로 · URL 쿼리의 임의 키를 객체에 펼침 · 화면 경로 파라미터를 API 경로에 그대로 씀 | — | 감사 실패 로그의 줄바꿈 제거 · `Files.createTempDirectory`(소유자 전용) · 알려진 키만 URL 상태로 · 기업(8자리)·지역(5자리)·경보 ID 형식 검사 + API 클라이언트가 `..` `//` `\` 경로 거부 | CodeQL 재분석 |
| 41 | (재검증) 33번 수정 후에도 **web 을 거친 위조 XFF 가 감사 로그에 그대로 기록**되고, XFF 가 없으면 모든 사용자가 web 컨테이너 IP 하나로 보임 → 모든 사용자가 **레이트리밋 버킷 하나를 공유**(한 사용자가 전체를 429 로 막을 수 있음) | Next.js rewrites 프록시는 받은 XFF 를 전달만 하고 실제 접속 주소를 붙이지 않음 + 신뢰 대역에 게이트웨이 포함 | **edge(nginx) 를 유일한 진입점으로** — XFF 를 실제 접속 주소로 덮어씀, api 는 고정 IP(10.213.0.10)의 edge 만 신뢰(`server.tomcat.remoteip.internal-proxies`), `/api/v1` 은 Next 를 거치지 않고 api 로 | 위조 요청 3가지(호스트 · 내부 컨테이너 · web 경유)의 기록 IP 비교, **E2E `X-Forwarded-For 위조는 감사 로그 IP 를 바꾸지 못함`** · k6 레이트리밋(컨테이너 IP 별 버킷) · 부하 처리량 445 → 766 req/s |

### 경보 백테스트 결과 (60거래일, 실데이터)

| 규칙 | 사용 표본 / 경보 | 평균 초과수익률 | 중앙값 | 업종보다 약했던 비율 | t |
|---|---|---|---|---|---|
| R-C01 부채비율 급등 | 6 / 22 | −0.9% | −5.6% | 50% | −0.05 |
| **R-C02 이자보상배율 < 1 연속** | 8 / 18 | **−17.2%** | −8.7% | 75% | **−2.52** |
| R-C03 영업현금흐름 적자 연속 | 5 / 12 | −3.0% | −8.8% | 60% | −0.28 |
| R-C05 채무보증 잔액 과다 | 3 / 15 | −8.7% | −9.2% | 67% | −0.61 |
| 비교 기준 (신호 없는 모든 창) | 480 | −0.0% | −3.2% | 61% | −0.02 |

R-C02 만 비교 기준과 뚜렷이 다르다(표본 8건이라 확정할 수 없음). 제외 사유는 대부분 '앞 사건과 겹침'(같은 규칙·종목이 여러 분기 연속 참)과 '기간 미경과'.
원천 종가가 수정주가가 아니므로 창 안에서 상장주식수가 2% 넘게 바뀐 경우를 제외했다(대우건설처럼 자사주 소각으로 1.1% 바뀐 경우는 포함 — 일별 등락률로 실제 가격 변동임을 확인).

### 보안 확인 (실제 스택)

| 확인 | 결과 |
|---|---|
| 커넥션 계정 (`pg_stat_activity`) | api · worker 모두 `buildrisk_app` (마이그레이션만 소유자 계정) |
| 호스트에서 api 직접 접근 | 연결 불가 (포트 미공개) · edge 경유만 |
| `X-Forwarded-For` 위조 | 호스트 · 내부 컨테이너 모두 실제 접속 주소로 기록 (41번) |
| 응답 헤더 | web: CSP(script-src 'self' + 카카오) · X-Frame-Options DENY · nosniff · Referrer-Policy · Permissions-Policy / api: CSP default-src 'none' |
| CSRF · 세션 쿠키 | XSRF-TOKEN SameSite=Strict · BR_SESSION HttpOnly · SameSite=Strict (E2E 로 확인) |
| 익명 변경 요청 | 401 · 감사 로그에 남음 |
| 카카오 지도 + CSP | 정상 표시, 콘솔 위반 0 |
| Grafana 익명 API | 401 |

## 3차 검증 (2026-09-26)

**방법** API 1,051개 경로 전수 호출(모두 2xx) + 경계값 23개(22개는 형식을 갖춘 4xx, 알 수 없는 출처 필터 1개는 빈 목록 200) · 18개 화면을 390 px 폭으로 열어 가로 넘침 측정 ·
역할별 쓰기 흐름 16단계 · 경보 근거 문장을 실데이터로 읽어 보기 · k6 부하를 여러 번 연속으로 걸며 api 메모리(RSS · 힙 · 힙 밖)를 3초마다 기록.

### 찾아서 고친 문제

| # | 증상 | 원인 | 해결 | 고정 · 확인 |
|---|---|---|---|---|
| 42 | `/mapping/unmapped` 2.1초 — 느린 쿼리 로그에서 유일하게 500 ms 초과 | `fs_std.period_key` 는 `char(6)` 인데 계산한 키는 `text` → 비교할 때 PK 인덱스를 못 씀 | 계산한 키를 `char(6)` 로 cast | 쿼리 1.3초 → 0.1초 |
| 43 | 지도에서 지표를 바꿀 때마다 1.95 MB(gzip 546 KB) | 연 1회 바뀌는 경계와 매월 바뀌는 값을 한 응답에 묶음 | 경계(버전 URL · immutable)와 값 목록(약 5 KB) 분리 ([ADR-020](adr/020-map-boundaries-cache.md)) | 지표 전환 546 KB → 5 KB · 재방문 304 |
| 44 | 경계 응답이 압축되지 않음 | Tomcat 은 강한 ETag 응답을 압축하지 않음 | 약한 ETag (이후 49번에서 미리 압축한 바이트로) | gzip 적용 확인 |
| 45 | web 컨테이너를 다시 만들면 edge 가 502 | nginx 가 upstream 이름을 시작할 때 한 번만 해석 → 옛 IP | `resolver 127.0.0.11` + `zone` + `resolve` (10초마다 재해석) | web 재생성 뒤 자동 복구 |
| 46 | (검토) web · edge 에 healthcheck 가 없어 edge 가 web 준비 전에 트래픽을 받을 수 있음 | — | web(`/api/config`) · edge(`/healthz`) healthcheck, edge 는 web 이 healthy 일 때 시작 | `docker compose ps` |
| 47 | **부하 중 api 가 죽음** — 처리량 16 req/s · p99 30초 이상 뒤 connection refused | 컨테이너 메모리 한도가 없어 JVM 최대 힙이 도커 VM 전체(7.7 GB, 다른 스택과 공유)의 75% = 6.2 GB. 부하 15초 만에 RSS 424 MB → 1.66 GB, VM 메모리 부족으로 도커가 43초 멈추고 Redis 응답이 끊긴 뒤 커널 OOM 킬(`exit 137`) | 모든 서비스에 `mem_limit` ([ADR-021](adr/021-container-memory.md)) | 3초 간격 RSS 기록 · 컨테이너 로그의 exit 137 |
| 48 | 한도(1 GB)를 걸어도 반복 부하 세 번째에 다시 OOM 킬 | 힙 768 MB(75%) + 힙 밖 약 300 MB > 1 GB. 게다가 1.8 GB 미만이면 JVM 이 **GC 를 Serial 로 바꿈**(힙 풀 이름 `Tenured Gen` 으로 확인), 이미지 ENTRYPOINT 의 명령줄 플래그가 compose 설정을 덮어씀 | 이미지 한 곳에 `-XX:+UseG1GC -XX:MaxRAMPercentage=60 -XX:+ExitOnOutOfMemoryError` | 3회 연속 부하: 최대 RSS 625 MiB / 1 GiB · 힙 밖 137 MB · 재시작 0 |
| 49 | 큰 응답이 요청마다 메모리를 크게 만듦 | Redis 캐시가 문자열 응답을 **JSON 문자열로 한 번 더 감싸** 저장 → 요청마다 1.9 MB 를 읽고 이스케이프 해제 · 재인코딩 · 압축. 옛 `/regions/geojson` 은 화면이 쓰지 않는데 요청 하나로 1.9 MB 를 만듦. `simplify` 에 아무 값(0~5000)이나 받아 값마다 PostGIS 단순화 + 캐시 항목 | 경계는 프로세스 안에 UTF-8 · gzip 바이트로 버전당 한 번(`PayloadCache`), 옛 경로 삭제, `simplify` 는 100 · 500 · 1000 만 | `ApiIT` (gzip · 원본 동일 · 304 · immutable · 400) · 최악 조건(50 VU 가 경계 전체 반복) 205 req/s · 111 MB/s · 오류 0 |
| 50 | 메모리를 고친 뒤에도 부하 중 db CPU 152%, 목록 API p95 300~600 ms | 부하의 대부분인 목록 5종(기업 · 경보 · 대시보드 · 지역 · 지역 시계열)이 캐시 없이 매번 DB 조회. 기업 목록 CTE 가 인라인돼 기업마다(43회) 집계 | 목록을 Redis read-through 캐시로(기본 조합만), 무효화를 **모든 Job 이 끝나는 한 곳**으로 옮김(전에는 Job 5개만 — 공시 · 미분양 · 가격지수 뒤에는 무효화 없음), CTE `MATERIALIZED` ([ADR-022](adr/022-read-cache.md)) | 13.8 → 2.1 ms · `RuleEvalJobIT` (캐시된 목록이 Job 직후 새 경보를 보여 줌) · **504 → 약 3,400 req/s, API p95 307 → 30 ms** |
| 51 | 지도가 탭 전환 · 화면 크기 변경 뒤 전국에 맞춰지지 않음 | 처음 그릴 때만 범위를 맞춤 | 크기 변화 감지(ResizeObserver)로 다시 맞춤(사용자가 움직였으면 유지) + '전국 보기' 버튼 | 화면 |
| 52 | 390 px 폭에서 표 · 차트가 있는 화면이 가로로 넘침 | CSS grid 항목의 기본 `min-width: auto` — 넓은 표가 칸을 밀어냄 | `.grid > * { min-width: 0 }` + 좁은 화면에서 보조 열 숨김 | 18개 화면 가로 넘침 0 (Playwright 측정) |
| 53 | 좁은 화면에서 경보를 눌러도 반응이 없어 보임 | 상세가 목록 아래에 있음 | 고르면 상세로 스크롤 | 화면 |
| 54 | 대시보드의 Job 이름이 화면 코드에 따로 있어 Job 을 추가하면 어긋남 | 목록 중복 | 서버의 Job 목록(`JobCatalog`)에서 이름을 내려줌 | — |
| 55 | 거래량 · ㎡당 가격 전년 대비(부호 있는 지표)가 한 방향 색으로 칠해져 감소가 '위험 낮음'처럼 보임 | 부호 있는 지표 목록에서 빠짐 | 발산형 색(0 = 중립) | 화면 |
| 56 | worker 를 재시작(배포)하면 돌던 Job 이 강제 종료될 수 있음 | 종료 처리 없음 + 도커 기본 유예 10초 | 종료 신호에 청크 경계에서 STOPPED(최대 45초), 다시 요청하면 이어감 · `stop_grace_period: 60s` ([ADR-012](adr/012-api-worker-queue.md)) | `AptTradeJobIT` — 멈춤 → 재요청 → COMPLETED, 호출 72회 · 재호출 0 |
| 57 | **이자보상배율 오경보 2건** — 영업이익이 양수인데 R-C02 | 분기 이자비용이 음수: 1분기 보고서엔 '이자지급', 반기엔 '이자지급(영업)'만 잡혀 누적이 줄어듦(6.2억 → 4.8억) → 누적 차분 음수 → 부호가 뒤집힌 배율. 16개사 28개 값 | 음수일 수 없는 분모가 음수면 값 대신 `INCONSISTENT` (두 경보는 이미 RESOLVED 로 닫혀 백테스트에는 들어가지 않았음을 확인) | `MetricCalculatorTest` |
| 58 | 경보 문장 "2025Q2 -6,049,063,302배" | 분기 이자비용 5원(누적 247,361,153 → 247,361,158원), 영업손실 302억 — 계산은 맞지만 영업손실이면 배율 크기는 뜻이 없음 | 영업손실 분기는 '영업손실'로 쓰고 숫자는 관측값에 그대로. 기업 화면 차트 축은 −10~20배로 자르고 툴팁은 실제 값 | `RuleEvaluatorTest` |
| 59 | 근거 문장의 숫자를 읽기 어려움: 천 단위 구분 없음, "21,250억원", 미분양 "20,950% 늘었고"(4호 → 842호), "-566억원로" | — | 천 단위 구분 · 조/억/만원(2조 1,250억원) · 기저가 작으면 늘어난 호수를 함께(838호(20,950%)) · 조사 | `RuleEvaluatorTest` (금액 6사례 · R-R01 문장) |
| 60 | 감사 로그: 거부된 요청(401 · 403)이 실제 경로(`/api/v1/rules/R-C01`)로 남아 행위별로 묶이지 않음 | 인가 단계에서 거부되면 핸들러가 정해지지 않아 경로 템플릿이 없음 | MVC 매핑에서 템플릿을 찾아 기록 | `SecurityIT` |
| 61 | k6 부하 스크립트가 화면이 더 이상 쓰지 않는 GeoJSON 을 7% 섞어 측정 | — | 실제 화면 호출(지표 목록 5% · 경계 재검증 304 2%)로 | — |

### 부하 결과 (k6 50 VU · 90초, edge 경유, 같은 기계)

| 시점 | 처리량 | API p95 | 오류 | api 메모리 |
|---|---|---|---|---|
| 점검 시작 (한도 없음 · 목록 캐시 없음) | 504 req/s | 307 ms | 0 — 이어서 돌리면 OOM 킬 | 최대 힙 6.2 GB |
| 한도 1 GB + 힙 75% | 566 req/s | 256 ms | 0 → 세 번째에 OOM 킬 | RSS 한도 도달 |
| **최종** (힙 60% · G1 · 경계 프로세스 캐시 · 목록 캐시) | **약 3,400 req/s** (3회 3,467 · 3,400 · 3,353) | **30 ms** | **0** / 약 92만 건 | 최대 RSS 625 MiB · 재시작 0 |
| 최악 조건: 50 VU 가 캐시 없이 경계 전체(gzip 530 KB) 반복 | 205 req/s · 111 MB/s | — | 0 | 한도 안 |

같은 기계에서 다른 프로젝트의 스택이 함께 돌고 있어 절대값은 흔들린다(부하 평균 6~15). 비교는 같은 날 연달아 잰 값끼리.

